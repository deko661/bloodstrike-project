#pragma once
// Inter Regular font - embedded binary
// Modern, clean sans-serif font for professional UI

static const unsigned char inter_regular_compressed_data[] = {
    // This is a placeholder - we'll use a simpler approach by loading from Windows fonts
    // or we can embed a minimal font. For now, let's use a system font approach.
};

// For this implementation, we'll use a built-in approach with better fonts
// We'll configure ImGui to use a better default font with larger size
